# Never commit a real private key. This file is only for the optional onchain companion.
# Use a new testnet-only wallet, funded with test STT and test USDC.
PRIVATE_KEY=0x...
RPC_URL=https://dream-rpc.somnia.network
WS_RPC_URL=wss://api.infra.testnet.somnia.network/ws
INDEXER_URL=https://dev.smk.somnia.host/v1/graphql

# Choose the direction that matches the receipt you created in the website.
# BUY_LATER uses UP. SELL_LATER uses DOWN.
PLAN=BUY_LATER
ASSET=ETH
PROTECTION_AMOUNT=1

